/*
	Unicode!
	Switch between modes with ease!
	1 = wireless mode
	0 = Wired mode!
*/
#include <powertop.h>

int MODE = 0;

if MODE = 0;
{
#include "alert-system-code_WIRED.c++"
}
else 
{
#include "neosensor.c"
}

